Drop your own images here:

- portrait.jpg    -> used in the hero section
- project-1.jpg   -> first project card
- project-2.jpg   -> second project card
- project-3.jpg   -> third project card

If a file is missing, the UI just hides the broken image (hero) or
shows a soft pink placeholder block (project cards), so the site
still looks fine without them.
